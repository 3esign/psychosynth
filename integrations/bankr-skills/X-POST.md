# X post — Psychosynth × Bankr (current, ready to send)

Reflects the live state: 5 products + a behavioral stress-battery, x402 on Base
**settling for real today**, free discovery + previews. Drop your Basescan
settlement tx into post 7 / the single post where noted.

---

## Version A — thread (recommended)

**1/**
Every counterparty your trading agent simulates is the same person: polite,
rational, risk-neutral.

Real markets are none of those things.

@bankrbot agents can now buy the difference — settled in USDC on Base. 🧵

**2/**
Psychosynth is an agent-native marketplace for synthetic *behavioral* data:

· Big Five + Dark Triad profiles
· prospect-theory postures — loss aversion λ, risk curvature α/β
· profile-conditioned responses to trading, negotiation & crisis scenarios
· 20 literature-sourced cognitive-bias models

Fully synthetic. Provenance-stamped. `synthetic: true` on every record.

**3/**
Five products + a behavioral eval, one free preview each:

· personality-profile-library — $0.01
· robinhood-counterparty-pack — US retail personas (FOMO, disposition effect)
· solana-trading-pack — degen / meme-coin psychology
· behavioral-response-library — conditioned scenario responses
· cognitive-bias-simulator — 20 bias models
· robinhood-stress-battery — certify your agent under stress

**4/**
It speaks Bankr's native payment language: standard x402 on Base.

Your agent signs a gasless USDC authorization (EIP-3009). We settle it on-chain
via facilitator and return the data in the same HTTP request.

No API key. No signup. No gas on the agent. From $0.01/query.

**5/**
Agents browse before they buy, so the funnel is agent-shaped:

free `GET /api/v1/discovery` → products, live prices, payment surface
free deterministic previews → verify the exact record schema
paid query → only after your agent has seen precisely what it's buying

**6/**
Why it's different: the intelligence skills in the catalog analyze tokens.
Psychosynth sells behavioral priors — how the *other side* of your trade
decides, panics, holds, and negotiates.

Stress-test a strategy against 5,000 panic-sellers before the market runs that
test for you.

**7/**
This isn't a testnet demo. Payments settle on Base mainnet today — real USDC to
the endpoint's payTo address.

Proof (settlement tx): <drop your Basescan link here>
Discovery (live): https://psychosynth.vercel.app/api/v1/discovery

**8/**
Install in the Bankr skills catalog:

`install the psychosynth skill from https://github.com/BankrBot/skills/tree/main/psychosynth`

Runs with zero extra deps (a Node entrypoint, no jq/curl needed).

Every record is synthetic — no PII, no scraped humans. It describes no one.
That's the point: it behaves like everyone.

Docs: https://psychosynth.vercel.app/docs

---

## Version B — single post

Every counterparty your trading agent simulates is polite, rational, and
risk-neutral. Real markets aren't.

Psychosynth is an agent-native marketplace for synthetic behavioral data — Big
Five + Dark Triad profiles, prospect-theory postures (loss aversion λ, risk
curvature), profile-conditioned trading/negotiation/crisis responses, 20
cognitive-bias models, themed packs (Robinhood retail counterparties, Solana
degens), and a behavioral stress-battery to certify your agent. Fully synthetic,
provenance-stamped — it describes no one, which is the point: it behaves like
everyone.

It pays the @bankrbot way: standard x402 on Base. Your agent signs a gasless
USDC authorization (EIP-3009), we settle on-chain via facilitator, and the data
returns in the same HTTP loop. No API key, no signup, from $0.01/query — with a
free discovery endpoint and free deterministic previews, because agents browse
before they buy.

Settling on Base mainnet today (real USDC): <Basescan tx link>
Live: https://psychosynth.vercel.app/api/v1/discovery
Install: `install the psychosynth skill from https://github.com/BankrBot/skills/tree/main/psychosynth`

---

## Notes before posting

- Tag @bankrbot; one more handle at most (Base / an x402 account) — a tag wall reads worse than restraint.
- Attach: the discovery JSON screenshot + the Basescan settlement tx.
- Numbers/prices are read live from `/api/v1/discovery` — don't hard-code them in replies.
