# X announcement — Psychosynth × Bankr ecosystem

Two versions below. The thread is recommended (more surface area, each post
quotable alone). Post AFTER the deploy + smoke test in HANDOFF.md Step 1; if
you complete the paid-call proof (Step 2), drop the Basescan link into post 3
and the single-post version.

Suggested attachments: screenshot of the homepage "Built for autonomous
agents" section, screenshot of the /api/v1/discovery JSON, and the Basescan
settlement tx.

---

## Version A — thread (recommended)

**1/**
Every simulated counterparty your agent trades against is the same person:
polite, rational, risk-neutral.

Real markets are none of those things.

Psychosynth sells the difference — and it now speaks the @bankrbot
ecosystem's native payment language. 🧵

**2/**
Psychosynth is an agent-native data marketplace for synthetic psychometrics:

· Big Five + Dark Triad personality profiles
· prospect-theory postures (loss aversion λ, risk curvature α/β)
· profile-conditioned responses to high-stakes trading, negotiation & crisis scenarios
· 20 literature-sourced cognitive-bias models

Human-curated, provenance-stamped, `synthetic: true` on every record.

**3/**
What shipped today: full standard x402 support on Base.

Your agent signs a gasless USDC authorization (EIP-3009). Our server settles
it on-chain via facilitator and returns records in the same HTTP loop.

No API key. No signup. No gas on the agent's side. From $0.01 per query.

**4/**
The funnel is agent-shaped, because agents browse before they buy:

free `GET /api/v1/discovery` → products, live prices, payment surface
free deterministic previews → verify the exact record schema
paid query → only after your agent has seen precisely what it's buying

**5/**
Why this matters for Bankr agents specifically: the intelligence skills in
the catalog analyze tokens. Psychosynth sells something nobody else does —
behavioral priors. How the participants on the other side of your trade
actually decide, panic, hold, and negotiate.

**6/**
Concrete uses:

· stress-test a strategy against 5,000 panic-sellers before the market runs
  that test for you
· red-team a negotiation agent against high-Machiavellianism personalities
· populate prediction-market and social sims with heterogeneous populations
· audit decision loops against loss aversion, anchoring & 18 other biases

**7/**
The psychosynth skill has been submitted to the Bankr skills catalog — once
merged it's one line:

`install the psychosynth skill from https://github.com/BankrBot/skills/tree/main/psychosynth`

Until then, everything is already live for any x402-capable agent:
https://psychosynth.vercel.app/api/v1/discovery

**8/**
On data ethics, since it matters: every record is fully synthetic. No PII, no
scraped humans, methodology linked from every response.

It describes no one. That's the point — it behaves like everyone.

Docs: https://psychosynth.vercel.app/docs

---

## Version B — single post

Simulated markets have a realism problem: every LLM-driven counterparty is
polite, rational, and risk-neutral. Real ones aren't.

Psychosynth is an agent-native marketplace for synthetic behavioral data —
Big Five + Dark Triad profiles, prospect-theory postures (loss aversion λ,
risk curvature), profile-conditioned responses to trading and negotiation
scenarios, and 20 literature-sourced cognitive-bias models. Human-curated,
provenance-stamped, fully synthetic (no PII — it describes no one, which is
the point: it behaves like everyone).

As of today it speaks the @bankrbot ecosystem's native payment language:
standard x402 on Base. Your agent signs a gasless USDC authorization
(EIP-3009), our server settles on-chain via facilitator, and the data comes
back in the same HTTP loop. No API key, no signup, from $0.01/query — with a
free discovery endpoint and free deterministic previews, because agents
browse before they buy.

While the catalog's intelligence skills analyze tokens, Psychosynth sells
behavioral priors — how the other side of your trade decides, panics, and
holds. Stress-test your strategy against 5,000 panic-sellers before the
market runs that test for you.

Skill submitted to the Bankr catalog; live now for any x402-capable agent:
https://psychosynth.vercel.app/api/v1/discovery

---

## Handles & hashtags

Primary tag: @bankrbot. Consider also (verify current handles before
posting): the Base ecosystem account and x402-related accounts. Restraint
reads better than a tag wall — @bankrbot + one more is plenty. Optional
hashtags: none, or a single $BNKR if you want token-search surface.
